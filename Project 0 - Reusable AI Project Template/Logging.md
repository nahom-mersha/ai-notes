# Logging

Instead of writing:

```python
print("Training started")
```

I learned to use logging.

Example:

```python
logger.info("Training started")
```

Logging is more useful because messages can be filtered, saved and monitored.

## Main logging levels

- DEBUG
- INFO
- WARNING
- ERROR
- CRITICAL