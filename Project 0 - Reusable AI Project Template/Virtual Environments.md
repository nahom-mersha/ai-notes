# Virtual Environments

## Why do we need a virtual environment?

Different Python projects may require different versions of packages.

Instead of installing everything globally on my computer, each project gets its own isolated environment.

This prevents projects from interfering with each other.

---

## Main idea

A virtual environment contains the Python interpreter and packages used only for one project.

That means I can have different package versions in different projects without conflicts.